import { setup, startDb, stopDb, createTables, deleteTables } from "jest-dynalite";
import { resolve } from "path";
import "jest-dynalite/withDb";
import { createActor } from "./create-actor";
import {
  CreateActorInput,
} from "../../types";

/**
 * Sometimes dynalite tests can require a little additional
 * time when you are running a lot of them in parallel.
 */
jest.setTimeout(10000);

setup(resolve("./"));
beforeAll(startDb);
beforeEach(createTables);
afterEach(deleteTables);
afterAll(stopDb);

test("Smoke test", async () => {
  const fixture : CreateActorInput = {
    firstName: 'foo',
    lastName: 'foo',
  };
  const result = await createActor(fixture);
  expect(result).toBeTruthy();
})
// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
